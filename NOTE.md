# 树与森林C++实现记录
## C++语法问题
- 变量名，引用，指针
    - 变量名：在传参时是拷贝对象，作为返回值时也是拷贝对象，非常不适合大型对象传递和返回，同时，在函数中无法将修改状态保存在对象中
    - 引用：引用就是一个别名，传参或返回时不会拷贝原始对象，只是拷贝别名，非常适合将修改状态保存到对象
    - 指针：指针时地址，完全不同于变量名和引用，通过new运算符出来的都是指针类型，因为时从堆中申请内存
    
- 友元
    - 谁是我的朋友就声明为友元
    - 我的友元可以直接访问我的私有属性
    
